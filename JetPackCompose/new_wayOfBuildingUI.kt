


/*
You're using Jetpack Compose, which is the newer way of building UIs in Android.
 That’s why there is no activity_main.xml layout file — because Jetpack Compose doesn’t use XML layouts at all!


🤔 Want to switch back to XML layouts?
If you want to use XML-based layouts (for learning or UI designer tool), here’s how:

Right-click res/layout → New → Layout Resource File.

Name it activity_main.xml.

Inside MainActivity.kt, replace the setContent {} block with:
setContentView(R.layout.activity_main)







*/